import { RemovecommasPipe } from './removecommas.pipe';

describe('RemovecommasPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovecommasPipe();
    expect(pipe).toBeTruthy();
  });
});
