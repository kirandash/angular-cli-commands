import { CustomtextareaDirective } from './customtextarea.directive';

describe('CustomtextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomtextareaDirective();
    expect(directive).toBeTruthy();
  });
});
